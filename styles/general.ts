import { StyleSheet } from 'react-native';

const generalStyles = StyleSheet.create({
  generalInputs: {
    paddingHorizontal: 15,
    paddingVertical: 15,
    borderColor: 'white',
    borderWidth: 1,
    borderRadius: 5,
    marginBottom: 20,
    fontSize: 16,
    color: 'white',
  },
  generalLabels: { color: 'white', fontSize: 16, marginBottom: 10 },
  generalTitle: {
    fontSize: 18,
    color: 'white',
    marginBottom: 20,
    fontWeight: 'bold',
  },
});

export default generalStyles;
